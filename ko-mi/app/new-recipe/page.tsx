const NewRecipe = ({ searchParams }) => {
  // how to pass value into this page??
  console.log("here: ", searchParams);

  // do all of the work here...
  // then let the user verify the recipe data and send to DB
  return (
    <div>
      <h1>New Recipe</h1>
    </div>
  );
};

export default NewRecipe;
