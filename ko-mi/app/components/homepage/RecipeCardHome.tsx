import Card from "@mui/material/Card";
import CardContent from "@mui/material/CardContent";

const RecipeCard = ({ recipe }) => {
  return (
    <>
      <p>Build a card for: {recipe.name}</p>
    </>
  );
};

export default RecipeCard;
